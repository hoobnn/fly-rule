{
  "name": "fly-rule",
  "private": true,
  "packageManager": "pnpm@11.10.0",
  "type": "commonjs",
  "scripts": {
    "prepare": "husky",
    "lint": "ruff check scripts/ && ruff format --check scripts/ && markdownlint-cli2",
    "lint:fix": "ruff check scripts/ --fix && ruff format scripts/ && markdownlint-cli2 --fix"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7",
    "markdownlint-cli2": "^0.23.2"
  }
}
